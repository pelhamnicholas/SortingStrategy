#ifndef BUBBLESORT_H
#define BUBBLESORT_H

#include "container.h"

class BubbleSort {
    private:

    public:
        BubbleSort() {};
        ~BubbleSort() {};
        void sort(Container *);
};

#endif
